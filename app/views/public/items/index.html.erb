<h2><%= "商品一覧(全#{@items.count}件)" %></h2>
<% @items.each do |item| %>
  <%= link_to item_path(item) do %>
    <%= attachment_image_tag item, :image %>
  <% end %>
  <%= "#{item.name}" %>
  <%= "¥#{item.with_tax_price}" %>
<% end %>
